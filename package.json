{
  "name": "stakebet",
  "version": "2.0.0",
  "private": true,
  "description": "Controle profissional de apostas esportivas — Simples, Surebets e Duplo Green",
  "scripts": {
    "dev": "npx serve public",
    "build": "node scripts/build.js",
    "deploy": "npm run build && npx vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.0"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
